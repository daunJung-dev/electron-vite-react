import { CancelPaymentInput, PaymentCallback, VAN } from '../creditCardManager.type';

class KovanCreditCardManager implements VAN {
  constructor() {
    
  }

  cancelPayment(cancelData: CancelPaymentInput, callback: PaymentCallback): void {
      
  }

  pay(amount: number, callback: PaymentCallback): void {
      
  }
}